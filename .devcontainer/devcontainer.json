{
  "name": "co-lecione-dev-container",
  "image": "ghcr.io/ludmilalima/co-lecione:devcontainer",
  "forwardPorts": [
    4200,
    5200
  ],
  "postStartCommand": "cd client && npm install && npm run start:dev-container & (cd server && npm install && npm run start:dev)"
}
